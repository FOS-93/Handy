    <%# LOGO %>
    <div>
        <div class="logo">
         <%= image_tag "circulo.png" %>
        </div>
    </div>
  <%# BANNER %>
<div>
  <div class="d-flex justify-content-between position-relative" style="background: #305349; width: 100%;">
    <div class="banner">
      <div class="">
        <div class="banner-headders" style="margin: 6rem;">
          <h1>Handy!</h1>
          <h2>Solutions at hand</h2>
          <% if user_signed_in? %>
            <%= link_to "", new_user_session_path %>
          <% else %>
             <%= link_to "Sign in", new_user_session_path %>
          <% end %>
        </div>
        <div class="logo">
          <%= image_tag "sammy.png" %>
        </div>
      </div>
    </div>
  </div>
</div>

<div>
  <%# SEARCH SPACE %>
  <div class="container">
    <div class="search-box">
      <h3>Look for your Handyman</h3>

      <%= simple_form_for :search, url: root_path, method: :get do |f| %>
        <div class="form-group">
          <input class="grow" type="text" name="search[query]" />
          <button type="submit" class="btn btn-success d-flex">
            <i class="fas fa-search"></i> Search
          </button>
        </div>
      <% end %>
    </div>
  </div>
</div>
<br>
<br>
<br>
<br>
<br>
<div>
  <%# CARDS SPACE %>
  <div class="container">
    <div class="cards">
      <% @handyman.each do |handyman|%>
        <%= render 'shared/handyman_card', handyman: handyman %>
      <% end %>
    </div>
  </div>
  <%= link_to "Find a Handy", handymen_path %>
</div>
